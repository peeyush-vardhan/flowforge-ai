# API Reference (Placeholder)

The FlowForge AI API accepts natural language prompts and returns typed workflow graphs.

See the FastAPI `/docs` endpoint in the `apps/api` service for live, interactive API documentation.

